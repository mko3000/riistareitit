import { fi, type Messages } from './fi'

// RII-7: the active UI strings. Finnish only for now — the English toggle
// (RII-14) picks between fi and en here, so components never change.
export const t: Messages = fi
